---
phase: 73-scene-environment-dead-code
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - interactive_gym/examples/footsies/scenes.py
  - interactive_gym/examples/cogrid/pyodide_overcooked/ (DELETE directory)
autonomous: true

must_haves:
  truths:
    - "The broken import in footsies/scenes.py is fixed (pyodide_overcooked reference removed)"
    - "Dead scene variables footsies_low_diff_initial_challenge_scene and footsies_low_diff_final_challenge_scene are removed"
    - "The empty pyodide_overcooked/ directory is deleted"
    - "The footsies/scenes.py module imports successfully"
    - "All unit tests pass after removals"
  artifacts:
    - path: "interactive_gym/examples/cogrid/pyodide_overcooked/"
      state: "deleted"
    - path: "interactive_gym/examples/footsies/scenes.py"
      provides: "All footsies scene definitions used by footsies_experiment.py (dead scenes and broken import removed)"
  key_links:
    - from: "interactive_gym/examples/footsies/footsies_experiment.py"
      to: "interactive_gym/examples/footsies/scenes.py"
      via: "import scenes"
      pattern: "from interactive_gym.examples.footsies import scenes"
---

<objective>
Fix broken import and remove dead code from example configurations: remove the broken pyodide_overcooked import from footsies/scenes.py, delete the empty pyodide_overcooked/ directory, and remove unused scene variables.

Purpose: Fix a broken import that prevents footsies/scenes.py from being imported, remove ~100 lines of dead scene definitions that are defined but never used in any experiment configuration, and clean up an empty vestigial directory.

Output: All example configs can be imported without errors. Only live scene definitions remain. Zero functionality change to any working example.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-scene-environment-dead-code/73-RESEARCH.md

Key reference files:
@interactive_gym/examples/footsies/scenes.py
@interactive_gym/examples/footsies/footsies_experiment.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix broken import and remove dead scene variables in footsies/scenes.py</name>
  <files>
    interactive_gym/examples/footsies/scenes.py
  </files>
  <action>
    1. In `interactive_gym/examples/footsies/scenes.py`, REMOVE the broken import on lines 12-14:
       ```python
       from interactive_gym.examples.cogrid.pyodide_overcooked import (
           scenes as oc_scenes,
       )
       ```
       This import references `interactive_gym/examples/cogrid/pyodide_overcooked/` which is an empty directory (only contains `__pycache__`, no Python source files). The `oc_scenes` alias is imported but NEVER used anywhere in the file (confirmed by grep). This broken import prevents the footsies/scenes.py module from being imported at all.

       Note: While unused imports are technically out of scope for Phase 73, this is a BROKEN import that causes an ImportError, not merely unused. It must be fixed because it prevents the module from loading, which blocks dead code analysis and makes the footsies example non-functional.

    2. Remove the `footsies_low_diff_initial_challenge_scene` variable (lines 210-307).
       - This is a complete FootsiesScene definition (~97 lines) that is NEVER referenced in `footsies_experiment.py` or any other file
       - It is only used as a source for `footsies_low_diff_final_challenge_scene` (which is also dead)

    3. Remove the `footsies_low_diff_final_challenge_scene` variable (lines 748-752).
       - This is a `copy.deepcopy(footsies_low_diff_initial_challenge_scene)` that is NEVER referenced in `footsies_experiment.py` or any other file
       - Both "low_diff" scenes are dead — they are not included in the stager in footsies_experiment.py

    4. After all removals, verify `footsies_experiment.py` can access all the scene variables it references:
       - `scenes.start_scene`
       - `scenes.footsies_initial_survey_scene`
       - `scenes.footsies_tutorial_scene`
       - `scenes.footsies_initial_challenge_intro`
       - `scenes.footsies_initial_challenge_scene`
       - `scenes.footsies_initial_challenge_survey_scene`
       - `scenes.footsies_dynamic_difficulty_rounds`
       - `scenes.footsies_controllable_difficulty_rounds`
       - `scenes.footsies_high_skill_rounds`
       - `scenes.footsies_low_skill_rounds`
       - `scenes.footsies_random_difficulty_rounds`
       - `scenes.footsies_dynamic_empowerment_rounds`
       - `scenes.footsies_empowerment_rounds`
       - `scenes.footsies_training_survey_scene`
       - `scenes.footsies_mc_survey`
       - `scenes.footsies_final_challenge_intro`
       - `scenes.footsies_final_challenge_scene`
       - `scenes.footsies_end_survey_scene`
       - `scenes.footsies_end_scene`

    5. Run `pytest tests/unit/ -v` to confirm all 27 tests pass.
  </action>
  <verify>
    - `grep -n "pyodide_overcooked" interactive_gym/examples/footsies/scenes.py` returns NO matches
    - `grep -n "oc_scenes" interactive_gym/examples/footsies/scenes.py` returns NO matches
    - `grep -n "footsies_low_diff" interactive_gym/examples/footsies/scenes.py` returns NO matches
    - `python -c "from interactive_gym.examples.footsies import scenes; print('OK')"` succeeds (previously would crash with ImportError)
    - `python -c "from interactive_gym.examples.footsies import scenes; assert hasattr(scenes, 'footsies_final_challenge_scene')"` succeeds
    - `pytest tests/unit/ -v` passes (27/27 tests)
  </verify>
  <done>
    Broken pyodide_overcooked import removed. Two dead scene variables (footsies_low_diff_initial_challenge_scene, footsies_low_diff_final_challenge_scene) removed. footsies/scenes.py can now be imported successfully. All unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete empty pyodide_overcooked/ directory</name>
  <files>
    interactive_gym/examples/cogrid/pyodide_overcooked/ (DELETE)
  </files>
  <action>
    1. DELETE the entire `interactive_gym/examples/cogrid/pyodide_overcooked/` directory.
       - This directory contains ONLY a `__pycache__/` folder — no Python source files
       - It was the target of the broken import in footsies/scenes.py (now removed by Task 1)
       - The original source files were apparently deleted or moved at some point, leaving only cached bytecode
       - No code anywhere references this directory (the only reference was the broken import in footsies/scenes.py, removed in Task 1)

    2. Verify no other files reference this path:
       `grep -r "pyodide_overcooked" interactive_gym/` should return NO matches (after Task 1's fix).
  </action>
  <verify>
    - `ls interactive_gym/examples/cogrid/pyodide_overcooked/` returns "No such file or directory"
    - `grep -r "pyodide_overcooked" interactive_gym/` returns NO matches
    - `pytest tests/unit/ -v` passes (27/27 tests)
  </verify>
  <done>
    Empty pyodide_overcooked/ directory deleted. No remaining references in codebase. All unit tests pass.
  </done>
</task>

</tasks>

<verification>
- The broken import `from interactive_gym.examples.cogrid.pyodide_overcooked import scenes` no longer exists
- `interactive_gym/examples/cogrid/pyodide_overcooked/` directory is deleted from disk
- `footsies_low_diff_initial_challenge_scene` and `footsies_low_diff_final_challenge_scene` no longer exist
- `python -c "from interactive_gym.examples.footsies import scenes"` succeeds (previously crashed)
- All scene variables used by footsies_experiment.py are still accessible
- `pytest tests/unit/ -v` passes with 27/27 tests
</verification>

<success_criteria>
- Zero broken imports remain in example code
- Zero dead scene variables remain in footsies/scenes.py
- Zero empty vestigial directories remain (pyodide_overcooked/ deleted)
- All 27 unit tests pass
- footsies/scenes.py can be imported without errors
</success_criteria>

<output>
After completion, create `.planning/phases/73-scene-environment-dead-code/73-02-SUMMARY.md`
</output>
